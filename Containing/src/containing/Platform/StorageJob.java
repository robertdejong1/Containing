package containing.Platform;

import containing.Container;

public class StorageJob {
    
    private int stripId;
    private Container container;
    
    public StorageJob(int stripId, Container container)
    {
        this.stripId = stripId;
        this.container = container;
    }
    
}
